package com.examhacker.resources

import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp

class Dimensions {
    companion object {
        val exampleUIDimension = 10.dp           // Remove later
        val exampleTypographyDimension = 10.sp   // Remove later
    }
}